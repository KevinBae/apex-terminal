---
description: Build and Review the Vite project for correctness.
---

1. Use the "Vite Build and Review" skill to run the build process and analyze results.
// turbo
2. Run `npm run build` in the project root.
3. If the build fails, analyze the terminal output for syntax or dependency errors.
4. If the build succeeds, verify that the `dist/` folder exists and contains compiled assets.
5. Provide a summary of the build status and any relevant observations.
